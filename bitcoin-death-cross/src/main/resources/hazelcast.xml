<?xml version="1.0" encoding="UTF-8"?>
<hazelcast xmlns="http://www.hazelcast.com/schema/config"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.hazelcast.com/schema/config
           http://www.hazelcast.com/schema/config/hazelcast-config-3.10.xsd">

	<group>
		<name>bitcoin</name>
	</group>

    <management-center enabled="true">http://localhost:8080/hazelcast-mancenter</management-center>

	<network>
		<!-- Don't use default (5701) so this demo can co-exist with other cluster on same machine -->
		<port auto-increment="true">6701</port>
		<join>
			<multicast enabled="false" />
			<tcp-ip enabled="true">
				<member-list>
					<member>127.0.0.1:6701</member>
				</member-list>
			</tcp-ip>
		</join>
	</network>

	<!-- Keep history of changes to the named Map available to Jet -->
	<event-journal enabled="true">
		<mapName>prices-in</mapName>
		<!-- Keep 10000 per partition -->
		<capacity>2710000</capacity>
		<!-- Do not expire -->
		<time-to-live-seconds>0</time-to-live-seconds>
	</event-journal>

</hazelcast> 